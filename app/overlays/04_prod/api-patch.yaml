apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-all
  namespace: default
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: api-all
        image: sportdeets.azurecr.io/sportsdataapi:5006
        env:
        - name: APPCONFIG_LABEL
          value: "Prod"
