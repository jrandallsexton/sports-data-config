apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-all
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: api-all
        env:
        - name: APPCONFIG_LABEL
          value: "Prod"
