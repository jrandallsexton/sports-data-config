apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-all
  namespace: default
spec:
  replicas: 0
  template:
    spec:
      containers:
      - name: api-all
        image: sportdeets.azurecr.io/sportsdataapi:4691
        env:
        - name: APPCONFIG_LABEL
          value: "Prod"
