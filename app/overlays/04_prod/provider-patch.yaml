apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-football-ncaa-worker
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: provider-football-ncaa-worker
        image: sportdeets.azurecr.io/sportsdataprovider:3944
        env:
        - name: APPCONFIG_LABEL
          value: "Prod"
        resources:
          requests:
            memory: 1Gi
            cpu: "0.5"
          limits:
            memory: 2Gi
            cpu: "2"
