apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-baseball-mlb-worker
  namespace: default
spec:
  # NO replicas here - scaled by KEDA (provider-baseball-mlb-worker-scaler).
  # Setting it makes Flux reassert replicas: 1 every 5-minute reconcile and
  # fight the HPA, killing pods mid-job on every cycle.
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/restartedAt: "2026-04-08T11:30:00Z"
    spec:
      containers:
      - name: provider-baseball-mlb-worker
        image: sportdeets.azurecr.io/sportsdataprovider:6765
        env:
        - name: APPCONFIG_LABEL
          value: "Prod"
