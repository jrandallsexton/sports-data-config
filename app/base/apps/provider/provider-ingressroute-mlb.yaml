apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: provider-baseball-mlb-https
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`provider.baseball-mlb.sportdeets.com`)
      kind: Rule
      services:
        - name: provider-svc-baseball-mlb
          port: 80
  tls:
    secretName: provider-baseball-mlb-sportdeets-tls
