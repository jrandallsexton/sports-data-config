apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: provider-baseball-mlb-worker-scaler
  namespace: default
  annotations:
    # Toggle paused: "true" + add paused-replicas: "0" to halt scaling.
    # KEDA treats paused-replicas as authoritative even when
    # paused: "false", so it MUST be removed (NOT left at "0") to
    # resume normal scaling — that's the gotcha that bit us on
    # 2026-04-25.
    autoscaling.keda.sh/paused: "true"
    autoscaling.keda.sh/paused-replicas: "0"
spec:
  scaleTargetRef:
    name: provider-baseball-mlb-worker
  pollingInterval: 15
  cooldownPeriod: 300
  minReplicaCount: 2
  maxReplicaCount: 4
  fallback:
    failureThreshold: 3
    replicas: 2
  triggers:
    - type: postgresql
      metadata:
        query: >-
          SELECT COUNT(*)::int
          FROM hangfire.jobqueue
          WHERE fetchedat IS NULL
            AND queue = 'default'
        targetQueryValue: "30"
        activationTargetQueryValue: "1"
      authenticationRef:
        name: provider-mlb-hangfire-trigger-auth
---
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: provider-mlb-hangfire-trigger-auth
  namespace: default
spec:
  secretTargetRef:
    - parameter: host
      name: provider-mlb-hangfire-pg-params
      key: host
    - parameter: port
      name: provider-mlb-hangfire-pg-params
      key: port
    - parameter: userName
      name: provider-mlb-hangfire-pg-params
      key: username
    - parameter: password
      name: provider-mlb-hangfire-pg-params
      key: password
    - parameter: dbName
      name: provider-mlb-hangfire-pg-params
      key: database
    - parameter: sslmode
      name: provider-mlb-hangfire-pg-params
      key: sslmode
