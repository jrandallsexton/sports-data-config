apiVersion: v1
kind: Service
metadata:
  name: provider-svc-baseball-mlb
  namespace: default
  labels:
    app: provider-baseball-mlb-api
spec:
  selector:
    app: provider-baseball-mlb-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
---
# Headless Service for Worker pods — enables Prometheus ServiceMonitor discovery
apiVersion: v1
kind: Service
metadata:
  name: provider-baseball-mlb-worker-metrics
  namespace: default
  labels:
    app: provider-baseball-mlb-worker
spec:
  selector:
    app: provider-baseball-mlb-worker
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  clusterIP: None
