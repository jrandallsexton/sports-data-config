apiVersion: v1
kind: Service
metadata:
  name: provider-svc-football-nfl
  namespace: default
  labels:
    app: provider-football-nfl-api
spec:
  selector:
    app: provider-football-nfl-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
---
# Headless Service for Worker pods — enables Prometheus ServiceMonitor discovery
apiVersion: v1
kind: Service
metadata:
  name: provider-football-nfl-worker-metrics
  namespace: default
  labels:
    app: provider-football-nfl-worker
spec:
  selector:
    app: provider-football-nfl-worker
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  clusterIP: None
