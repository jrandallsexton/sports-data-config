apiVersion: v1
kind: Service
metadata:
  name: metricbot
  namespace: default
spec:
  selector:
    app: metricbot
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
