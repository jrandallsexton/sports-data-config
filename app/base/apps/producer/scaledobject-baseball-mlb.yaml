apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: producer-baseball-mlb-worker-scaler
  namespace: default
  annotations:
    # Toggle paused: "true" to halt scaling. paused-replicas pins the
    # target replica count while paused; KEDA treats its presence as
    # authoritative even when paused: "false", so it must be removed
    # (NOT left at "0") to resume normal scaling.
spec:
  scaleTargetRef:
    name: producer-baseball-mlb-worker
  pollingInterval: 15
  cooldownPeriod: 300
  minReplicaCount: 2
  maxReplicaCount: 6
  fallback:
    failureThreshold: 3
    replicas: 2
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        # Up fast; down after 10 sustained quiet minutes, 1 pod / 5 min -
        # a terminating pod abandons in-flight Hangfire jobs. Rationale:
        # producer/scaledobject-football-ncaa.yaml.
        scaleUp:
          stabilizationWindowSeconds: 0
          policies:
            - type: Pods
              value: 4
              periodSeconds: 60
        scaleDown:
          stabilizationWindowSeconds: 600
          policies:
            - type: Pods
              value: 1
              periodSeconds: 300
  triggers:
    - type: postgresql
      metadata:
        # Outstanding work = unclaimed + in-flight, ALL queues (live
        # sourcing rides the sticky '00-live' queue). Full rationale:
        # producer/scaledobject-football-ncaa.yaml.
        query: >-
          SELECT COUNT(*)::int
          FROM hangfire.jobqueue
          WHERE (fetchedat IS NULL
             OR fetchedat > now() - interval '5 minutes')
        targetQueryValue: "30"
        activationTargetQueryValue: "1"
      authenticationRef:
        name: producer-mlb-hangfire-trigger-auth
---
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: producer-mlb-hangfire-trigger-auth
  namespace: default
spec:
  secretTargetRef:
    - parameter: host
      name: producer-mlb-hangfire-pg-params
      key: host
    - parameter: port
      name: producer-mlb-hangfire-pg-params
      key: port
    - parameter: userName
      name: producer-mlb-hangfire-pg-params
      key: username
    - parameter: password
      name: producer-mlb-hangfire-pg-params
      key: password
    - parameter: dbName
      name: producer-mlb-hangfire-pg-params
      key: database
    - parameter: sslmode
      name: producer-mlb-hangfire-pg-params
      key: sslmode
