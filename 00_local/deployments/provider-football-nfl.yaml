apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-football-nfl
  namespace: local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provider-football-nfl
  template:
    metadata:
      labels:
        app: provider-football-nfl
    spec:
      containers:
      - name: provider-football-nfl
        image: sportsdataprovider:latest
        imagePullPolicy: Never
        command: ["dotnet"]
        args: ["SportsData.Provider.dll", "-mode", "FootballNfl"]
        env:
          - name: APPCONFIG_CONNSTR
            valueFrom:
              secretKeyRef:
                name: az-app-config
                key: connectionstring
        envFrom:
          - configMapRef:
              name: app-config
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: 512Mi
            cpu: "1"
          requests:
            memory: 256Mi
            cpu: "0.2"